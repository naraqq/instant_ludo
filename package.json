{
  "name": "elemental-ludo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @ludo/client",
    "build": "npm run build -w @ludo/client",
    "preview": "npm run preview -w @ludo/client",
    "server": "npm run dev -w @ludo/server",
    "test": "npm run test -ws --if-present"
  }
}
